# Simple Chimera Configuration
# Minimal configuration for basic operation

[protocol]
command = "send_data"
target_id = "DEADBEEF"

[simulation]
message = "Hello"

[channel]
snr_db = 30.0  # Increased from 20.0 to confirm system works perfectly
link_loss_db = 0.0

[terminal]
telemetry_interval_secs = 1.0
